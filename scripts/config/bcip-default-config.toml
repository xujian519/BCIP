# BCIP Agent 专用配置（~/.bcip）
# 与本地 Codex 桌面版（~/.codex）完全隔离。
# 默认经本地 LiteLLM 代理 (8788) 使用 GLM / Kimi 编程套餐；如需直连第三方，请改 base_url / env_key。

model_provider = "LocalProxy"
model = "glm-5.1"
model_reasoning_effort = "medium"
disable_response_storage = true
model_context_window = 200000
model_auto_compact_token_limit = 180000
prevent_idle_sleep = true

[model_providers.LocalProxy]
name = "本地代理 (GLM + Kimi 编程套餐)"
wire_api = "chat"
base_url = "http://127.0.0.1:8788/v1/"
env_key = "OPENAI_API_KEY"

[model_providers.DeepSeek]
name = "DeepSeek"
wire_api = "chat"
base_url = "https://api.deepseek.com/v1/"
env_key = "DEEPSEEK_API_KEY"

[model_providers.ZhiPu]
name = "智谱 GLM"
wire_api = "chat"
base_url = "https://open.bigmodel.cn/api/paas/v4/"
env_key = "ZHIPU_API_KEY"

[model_providers.Kimi]
name = "Kimi Code"
wire_api = "chat"
base_url = "https://api.moonshot.cn/v1/"
env_key = "KIMI_CODE_API_KEY"

[features]
js_repl = false

[desktop]
auto_connect = true

# 内置知识库与 BGE-M3 语义检索（桌面端启动 bcip 时会注入 BCIP_* 环境变量）
# [assets]
# dir = "/Users/you/projects/BCIP/codex-rs/codex-patent-assets"
#
# [embedding]
# url = "http://127.0.0.1:8009"
# model = "bge-m3-mlx-8bit"
# api_key = "your-omlx-api-key"
