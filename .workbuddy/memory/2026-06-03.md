# 2026-06-03 工作日志

## 设计审查：BCIP 桌面端 vs Codex 像素级规范

- 对照 `docs/codex-desktop-pixel-perfect-design-spec.md` v2.0，全面审查 `apps/desktop/src/` 全部组件
- 审查范围：色彩系统、布局尺寸、字体排版、间距系统、圆角阴影、动画动效、组件细节
- 发现 **47 项差距**：P0(2) + P1(18) + P2(27)
- 主要问题：
  1. 色彩系统未用暖色调（#F5F2EE vs #F5F5F5）
  2. TitleBar 高度 40px（应 38px），StatusBar 28px（应 32px）
  3. AgentHeader 40px（应 48px）
  4. 圆角系统整体偏移（radius-sm 6px 应 2px）
  5. 消息气泡内边距过紧
  6. 设置页标题字号偏大
  7. 动画时长未统一（150ms vs 规范 100ms）
  8. 按钮组件未适配品牌绿
- 输出详细审查报告：`/Users/xujian/projects/BCIP/.workbuddy/design-audit-report.md`
- 建议按 P0->P1->P2 分批修复

## 设计修复：三阶段执行

### 第一阶段 P0 — 布局骨架（4 项）
- **TitleBar 高度**：确认已为 38px，统一 tailwind spacing.titlebar = 38px
- **StatusBar 高度**：28px -> 32px，统一 index.css --layout-statusbar-height = 32px
- **AgentHeader 高度**：--chat-header-h 40px -> 48px
- **布局变量统一**：tailwind.config.js spacing 与 CSS 变量同步

### 第二阶段 P1 — 设计系统核心（20+ 项）
- **色彩系统暖色化**：
  - 浅色：bg-base #F5F2EE, bg-surface #FAF8F5, bg-sidebar #F0EDE8
  - 深色：bg-base #1C1A18, bg-surface #1A1816, bg-sidebar #1A1816
  - 文字：text-primary #1A1814, text-secondary #6B6560, text-tertiary #A39E98
  - 边框：统一 rgba(0,0,0,0.08)
  - Hover：accent-primary-hover #3D6A5E -> #5A9A8C
- **圆角系统修正**：radius-sm 2px, md 6px, lg 8px, xl 10px, 2xl 12px, 3xl 16px
- **按钮品牌适配**：button.tsx default variant 使用 var(--accent-primary) + hover var(--accent-primary-hover)
- **Switch 尺寸**：h-[1.15rem] w-8 -> h-5 w-9 (20px x 36px)
- **动画时长统一**：fast 100ms, normal 150ms, slow 250ms
- **消息气泡内边距**：--chat-bubble-py 6px->10px, --chat-bubble-px 10px->14px
- **Composer 内边距**：px-2.5 -> px-4 (16px)，垂直方向同步增加
- **设置页标题字号**：pageTitle text-2xl(24px) -> text-xl(20px), sectionTitle 13px -> 16px
- **阴影系统补全**：添加 --shadow-floating, --shadow-modal, --shadow-drag
- **卡片圆角**：settingsTheme card rounded-xl -> rounded-2xl (12px)

### 第三阶段 P2 — 细节优化（10+ 项）
- **滚动条样式**：thumb rgba(0,0,0,0.16) -> 0.20，hover 0.24 -> 0.28
- **动画时长统一**：ReasoningBlock/ToolCallCard/TodoDock/LeftSidebar/StageIndicator/SearchView 中 duration-200/300 统一为 150/250ms
- **缓动函数**：保留 cubic-bezier(0.4, 0, 0.2, 1) 作为项目标准

### 修改文件清单
- `apps/desktop/src/index.css` — 色彩变量、布局变量、阴影、滚动条
- `apps/desktop/tailwind.config.js` — 色彩映射、圆角、间距、动画时长、阴影
- `apps/desktop/src/components/ui/button.tsx` — 品牌色适配
- `apps/desktop/src/components/ui/switch.tsx` — 尺寸修正
- `apps/desktop/src/components/StatusBar.tsx` — 高度修正
- `apps/desktop/src/components/agent/AgentHeader.tsx` — 高度已通过 CSS 变量修正
- `apps/desktop/src/components/agent/UserBubble.tsx` — 动画时长
- `apps/desktop/src/components/agent/AgentBlock.tsx` — 动画时长、border 过渡
- `apps/desktop/src/components/agent/Composer.tsx` — 内边距统一
- `apps/desktop/src/components/agent/ReasoningBlock.tsx` — 动画时长
- `apps/desktop/src/components/agent/ToolCallCard.tsx` — 动画时长
- `apps/desktop/src/components/todo/TodoDock.tsx` — 动画时长
- `apps/desktop/src/components/sidebar/LeftSidebar.tsx` — 动画时长
- `apps/desktop/src/components/stage/StageIndicator.tsx` — 动画时长
- `apps/desktop/src/components/center/SearchView.tsx` — 动画时长
- `apps/desktop/src/components/settings/codex/settingsTheme.ts` — 标题字号、卡片圆角

## 第二轮设计修复

### 新增修复项
- **AgentFooter 高度**：确认已为 32px (h-8)，已正确
- **代码块语言标签**：AgentBlock.tsx 改为右上角显示，带圆角和背景色
- **input focus ring**：3px -> 2px，对齐规范
- **sonner toast**：添加左侧彩色边框指示器（success/error/warning/info）
- **ThreadRow 高度**：34px -> 48px，与 DropdownThreadRow 一致
- **PDF 查看器背景色**：添加 `#E8E5E0` 作为页面渲染区背景
- **Markdown 标题字号**：H1 30px->24px, H2 24px->20px, H3 20px->18px
- **连接状态脉冲动画**：已连接状态圆点添加 `animate-pulse`
- **等宽字体**：确认 tailwind.config.js 中 `font-mono` 已映射到 JetBrains Mono
- **ReasoningBlock 默认折叠**：确认 defaultExpanded=false，动画 250ms

## 第三轮设计修复

### 新增修复项
- **按钮默认高度**：h-9 (36px) -> h-8 (32px)，对齐规范 Primary 按钮
- **按钮 focus ring**：3px -> 2px，与 input 一致
- **流式边框渐隐**：250ms -> 300ms，对齐规范
- **Terminal 背景**：`#1e1e1e` -> `#0D0D0D`，对齐规范
- **图片查看器信息叠加层**：添加底部文件名叠加层（半透明 + 模糊）
- **MCP 状态色**：确认已正确映射（ready=success, failed=error, cancelled=tertiary）

## 配置持久化功能实现

### 完成内容
1. **write_config Tauri command**（任务 #1）
   - `commands/config.rs` 新增 `write_config` command + `WriteConfigParams` 类型
   - `lib.rs` 注册 `write_config` 到 invoke_handler
   - Rust 侧 `write_bcip_config()` 已在 `config.rs` 中实现（读取→修改→写回策略）
2. **Login.tsx 对接 API Key 持久化**（任务 #2）
   - `handleApiKeySubmit` 改为 `async`，调用 `invoke('write_config', { params: { api_key } })`
   - DeepSeek OAuth handler 添加 TODO 注释（后续接入 OAuth 回调后持久化）
3. **ModelSettings.tsx 对接配置读写**（任务 #5）
   - `categories/ModelSettings.tsx` 从硬编码 state 改为：
     - useEffect 加载时 `invoke('read_config')` 读取已保存值
     - 模型/API Key 变更时通过 `invoke('write_config')` 持久化
4. **build.sh 知识库资产捆绑**（任务 #3）
   - 确认 `build.sh`、`prepare-assets.sh`、`tauri.conf.json` resources 配置均正确
   - 执行 `prepare-assets.sh` 成功暂存资产（patent_kg.db 316M, laws.db 158M 等）
5. **端到端验证**（任务 #4）
   - Rust `cargo check` 通过（仅 1 个无关 warning）
   - TypeScript `tsc --noEmit` 零错误通过

## 第四轮 UI 打磨（视觉品质提升）

### 修改文件清单
- `WelcomeScreen.tsx` — 完全重写空状态页面：品牌图标 + 功能标签徽章（带入场动画）+ 快速操作按钮
- `MessageTimeline.tsx` — 新增 `EmptyConversation` 组件：品牌图标 + 快捷操作建议 + 阶梯式入场动画
- `Composer.tsx` — 发送按钮增加 shadow 和 scale 反馈；SlashCommandPalette 改为 message-enter 动画 + 选中项高亮图标
- `ActivityBar.tsx` — 新增 `ActivityTooltip` 自定义 tooltip（替代原生 title），延迟 400ms 显示，带缩放过渡；活跃指示器增加弹簧动画
- `UserBubble.tsx` — 气泡圆角统一为 rounded-2xl rounded-br-md，添加 border 和 hover:shadow-md
- `AgentBlock.tsx` — 入场动画偏移从 2px 增至 3px，代码块语言标签改为右下角 + 底边框样式，代码块增加 hover:shadow-md
- `ToolCallCard.tsx` — inline 展开区域添加 message-enter 入场动画
- `StatusBar.tsx` — 主题切换按钮添加旋转过渡（360deg），连接状态圆点添加 transition-colors，进度条缓动改为 ease-out
- `WorkspaceTabs.tsx` — 标签页添加 transition-colors，选中态 font-weight: 500，关闭按钮改为 rounded-md
- `index.css` — 新增 5 个全局工具类/动画：panel-slide-in、fade-scale-in、cursor-blink、micro-press、brand-glow

### 设计改进要点
- 空状态页面（WelcomeScreen + EmptyConversation）从纯文字改为品牌图标 + 功能标签 + 引导操作
- 所有悬停/激活状态统一使用弹簧曲线 cubic-bezier(0.34, 1.56, 0.64, 1)
- ActivityBar 用自定义 Tooltip 替代原生 title，提升 macOS 原生感
- 消息气泡/代码块增加悬停微反馈（shadow 变化）
- TypeScript 编译零错误通过
